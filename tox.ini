[tox]
isolated_build = true
envlist = integration

[testenv:integration]
deps =
    pytest
    pytest-copier
commands =
    python -m pytest tests/integration/ --include-venv

[testenv:all]
deps =
    pytest
    pytest-copier
commands =
    python -m pytest tests/ --include-venv
